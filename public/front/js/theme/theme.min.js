'use strict';
 


 
 
//$('.md-radio label').on('click', function(event){
$('label').on('click', function(event){
  
  // $('.page').animate({
    
  //   scrollTop: $(this).offset().top - 300
  // }, );

    // var center = $('.page').height()/2;
    // var top = $(this).offset().top ;
    // if (top > center){
    //     $('.page').scrollTop(top-center);
    // }

    //var target = $(this).closest('.md-radio').find('.md-radio label');
    // var target = $(this);

    // //event.preventDefault();
    // $('.page').stop().animate({
    //     scrollTop: target.offset().top - 100
    // }, 1000);
    //alert($(this).position().top);
    $('.page').animate({scrollTop:$(this).position().top}, 'slow');

})

 


// $(document).ready(function() {
//   auto_grow(element);
// });

function auto_grow(element) {
  element.style.height = "5px";
  element.style.height = element.scrollHeight + "px";
}

/*********
 *Navigation Js START
 **************/

$(document).ready(function () {
    $('#sidebarCollapse').on('click', function () {
        $('.site-menubar-unfold').toggleClass('site-menubar-hide');
    });
});


/*********
 *Navigation Js END
 **************/

 /*********
 *Waves Js START
 **************/
var scrollingAnimations = $("body").data("scrolling-animations");
if (scrollingAnimations) {
  // new WOW().init();
}


// Waves.init();
 /*********
 *Waves Js END
 **************/

// $.fn.select2.amd.require(['select2/selection/search'], function (Search) {
//   var oldRemoveChoice = Search.prototype.searchRemoveChoice;
  
//   Search.prototype.searchRemoveChoice = function () {
//       oldRemoveChoice.apply(this, arguments);
//       this.$search.val('');
//   };
// });